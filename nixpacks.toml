[phases.setup]
nixPkgs = ['...', 'php81', 'php81Packages.composer']

[phases.build]
cmds = [
    'composer install --no-dev --optimize-autoloader'
]

[phases.deploy]
startCommand = 'php artisan migrate --force && php artisan db:seed --force && cd public && php -S 0.0.0.0:$PORT'
